// Program to check if a number is even or odd

// Input: Replace this number with any integer to test
let number = 2;

// Check if the number is even or odd
if (number % 2 === 0) {
    console.log(`${number} is an even number.`);
} else {
    console.log(`${number} is an odd number.`);
}
